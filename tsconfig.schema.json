{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Local tsconfig schema",
  "type": "object",
  "description": "Local fallback schema to avoid network requests from the editor",
  "additionalProperties": true
}
